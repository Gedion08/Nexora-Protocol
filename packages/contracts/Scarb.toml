[package]
name = "nexora_contracts"
version = "0.1.0"
edition = "2024_07"
cairo-version = "2.20.0"

[[target.starknet-contract]]
sierra = true

[dependencies]
starknet = "2.20.0"

[dev-dependencies]
# snforge is temporarily disabled until its git source is confirmed for this toolchain
