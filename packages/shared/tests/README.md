# Shared Package Tests

Tests for shared types, config, and utilities.

## Test Coverage

- `config.test.ts` — Config loader and environment variable parsing
- `types.test.ts` — Type definitions and runtime checks
- `retry.test.ts` — Retry utility with exponential backoff

## Running

```bash
pnpm test
```
